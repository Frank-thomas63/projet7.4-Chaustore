{% extends 'admin/base.html.twig' %}

{% block body %}
  <div class='container mt-4'>
    <h1> Edit brand </h1>
      <td><img src="/brand/{{ brand.logo }}" width="100" height="100"/></td>
      {{ include ('admin/brand/_form.html.twig', { button: 'edit'}) }}
  </div>
{% endblock %}
